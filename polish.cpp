#include <iostream>
#include <cmath>
#include <stack>

using namespace std;

//Function to evaluate postfix expressions

int evaluate_postfix (string expression){
    char c;
    int sizeString=0;
    stack 
    while (sizeString!= expression.size()){

        char=expression[sizeString];


        sizeString++;
    }


}